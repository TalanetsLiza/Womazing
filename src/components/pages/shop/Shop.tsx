import React from "react";
import PageTitle from "../../PageTitle/PageTitle";

const Shop: React.FC = () => {
    return (
        <div>
            <PageTitle titleKey="shop" />
        </div>
    );
};

export default Shop;